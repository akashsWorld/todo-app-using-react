import React from "react";

const Navbar = () => {
  return (
    <div>
      <nav className="flex h-20 py-5 px-5 bg-black bg-opacity-50">
        <div>
          <h1 className="text-4xl font-headLandOne">ToDo List</h1>
        </div>
      </nav>
    </div>
  );
};

export default Navbar;
